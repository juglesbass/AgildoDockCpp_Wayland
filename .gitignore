# Pasta de compilação local (CMake)
/build/

# Backups locais de desenvolvimento
/.backup/

# Metadados do editor / agente (não faz parte do código-fonte)
.cursor/

# Artefactos de empacotamento local (tarballs gerados por scripts)
*.tar.gz
*.pkg.tar.zst
/pkg/
/src/
packaging/aur/pkg/
packaging/aur/src/

# Imagens de referência / capturas de ecrã locais (não fazem parte do código)
/assets/

# Ficheiros temporários
*~
*.swp
*.user
